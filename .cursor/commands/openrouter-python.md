# openrouter-python

Write your command content here.

This command will be available in chat with /openrouter-python

在本项目生成 openrouter_client.py（按规则：启动先 GET /models，推理任务用 reasoning:effort=high，可 fallback），并在服务入口演示如何 init_openrouter_models() + 调用 chat()。