# scaffold

Write your command content here.

This command will be available in chat with /scaffold

以本仓库规则创建标准目录结构（apps/web, apps/admin, services/api, db/migrations, infra, docs, scripts），并生成 README、.env.example、compose.yaml 的最小可运行版本。前端用 React Vite TS + Tailwind；Admin 用 React-Admin；后端先给 Node(Fastify) 版本（如我指定其他语言再改）。要求：docker compose up -d 可跑通。
